All Hooks:
    /* Primary Hooks - scripts.new.js */
    void serverStartUp
    void serverShutDown
    void step
    void init

    beforeNewMessage(String message)
    afterNewMessage(String message)

    beforeLogIn(Number playerId)
    afterLogIn(Number playerId)

    beforeLogOut(Number playerId)
    afterLogOut(Number playerId)

    beforeChangeTeam(Number playerId, (Custom) Boolean afterLogIn)
    afterChangeTeam(Number playerId, (Custom) Boolean afterLogIn)

    (undefined) beforePlayerKick(Number src, Number tar)
    (undefined) afterPlayerKick(Number src, Number tar)

    (undefined) beforePlayerBan(Number src, Number tar)
    (undefined) afterPlayerBan(Number src, Number tar)


    beforeChatMessage(Number src, String message, Number chan, Boolean beforeCommandParsed)

    onCommand(Number src, String message, Number chan, String commandName, String data)

    (result) commandNameRequested(Number src, String message, Number chan, String commandName)
    (result) commandPlayerAuthRequested(Number src, String message, Number chan, String commandName)
    (result) commandInfoRequested(Number src, String message, Number chan, Object{CommandInfo} commandInfo)

    onCommandError(Number src, String fullCommand, Number chan, String errorType, Error Exception)

    allowedWhenMuted(Number src, String command, Number chan)

    beforeChannelDestroyed(Number chan)
    afterChannelDestroyed(Number chan)

    beforeChannelJoin(Number src, Number chan)
    afterChannelJoin(Number src, Number chan)

    (undefined) afterChannelCreated(Number chan, String name, Number src)


    /* Module hooks */
    onMute(Number src)
    onChannelKick(Number src, Number chan)
    onKick(Number src)

    /* Command hooks */
    notEnabled(Object{CommandInfo} command)
    onMessageCommand(Object{CommandInfo} command)